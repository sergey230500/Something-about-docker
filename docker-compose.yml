version: '3'

services:
  django:
    build: ./AAKPD
    container_name: django
    command: python manage.py runserver 0.0.0.0:8000
    ports: 
      - "8000:8000"
    depends_on:
      - db

  
  db:
    image: sqlite
    
  pythonbot:
    build: ./PythonBot
    depends_on:
      - db